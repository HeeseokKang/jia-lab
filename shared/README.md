# shared

## Purpose

This directory contains reusable utility code that is shared across multiple analysis workflows to reduce duplication and enforce consistent preprocessing logic.

## Key Files

- `utils.py`: Provides data-cleaning and file-selection helpers (e.g., channel detection, index normalization, and de-duplicated image list construction).
