# Large per-condition outputs — regenerated by src/dpc_analyze.py.
# Keep the aggregated master table in git, but skip the heavy arrays/figures.
analysis/**/*.npy
analysis/**/dpc_overview.png

# Python cruft
__pycache__/
*.pyc
