# configs

## Purpose

This directory centralizes project-wide configuration so analysis scripts can share a single source of truth for paths, dataset registration, and output locations.

## Key Files

- `paths.py`: Defines canonical NAS/scratch roots, dataset-specific raw-data paths, and output directories used by analysis pipelines.
